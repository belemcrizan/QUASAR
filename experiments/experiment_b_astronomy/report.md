# Experiment B - Astronomy

Status: registered synthetic POC. Run `python experiments/experiment_b_astronomy/run.py` after installation.

The experiment injects telescope-like transient precursors into synthetic flux, colour, spectral width, and local-background series. It evaluates the same core used by Experiment A. It does not claim discovery of a real astronomical object.

